# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tripplanner?schema=public"

# Base URL for the app (used for share links)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Secret key for encrypting stored credentials (IMPORTANT: change this in production!)
ENCRYPTION_SECRET="change-this-to-a-random-secret-key-32chars!!"
